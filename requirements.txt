# Installed on every run, including the ones that find no open window
# and exit in a second. Keep this list small for that reason: anything
# heavy belongs in requirements-media.txt instead.

requests>=2.32
beautifulsoup4>=4.12
PyYAML>=6.0
pdfplumber>=0.11
# Floor raised from 0.40 on 2026-08-10. Synthesis now passes thinking and
# output_config, and 0.40 predates both, so the old pin would have let a
# runner install a version that rejects the call the fix depends on.
anthropic>=0.69

# Renders the brief to HTML for the email. Without it the brief arrives
# as raw markdown, hashes and asterisks and pipe-delimited tables, which
# reads worse than plain prose would have. Pure python, no system
# dependencies, and run.py degrades to plain text if it is absent.
markdown>=3.5
